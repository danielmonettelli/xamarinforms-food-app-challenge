<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="XF_FoodApp.Views.AboutPage"
             xmlns:vm="clr-namespace:XF_FoodApp.ViewModels"
             Title="{Binding Title}">

    <ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>

    <Grid Style="{StaticResource GridSpacing}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollView Grid.Row="0"
                    Orientation="Vertical">
            <StackLayout
                     Orientation="Vertical">
                <Label Text="MyDemo" Style="{StaticResource TxtTit_Prim}"/>
                <Image Source="{StaticResource IconDemo}"/>
                <Image Source="{StaticResource IconDemo3}"/>
                <Image Source="{StaticResource IconDemo4}"/>
                <Image Source="{StaticResource IconDemo5}"/>
                <Image Source="{StaticResource IconDemo6}"/>
            </StackLayout>
            
        </ScrollView>
    </Grid>
    
</ContentPage>
